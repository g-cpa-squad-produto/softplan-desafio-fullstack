export default class Processo {

  constructor(id, numero, descricao, statusParecer, usuariosPararecer) {
    this.id = id;
    this.numero = numero;
    this.descricao = descricao;
    this.statusParecer = statusParecer;
    this.usuariosPararecer = usuariosPararecer;
  }

}