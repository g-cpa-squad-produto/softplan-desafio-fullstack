version: '3'
services:

  processos-api:
    image: adolfo-hengling/processos-api:1.0.0
    container_name: processos-api
    ports:
      - 8081:8081
    networks:
      - processos-network

  processos-app:
    image: adolfo-hengling/processos-app:1.0.0
    container_name: processos-app
    ports:
      - 8080:80
    networks:
      - processos-network

networks:
  processos-network: