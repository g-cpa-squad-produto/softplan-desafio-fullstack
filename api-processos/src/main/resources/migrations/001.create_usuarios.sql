--liquibase formatted sql

--changeset marcos.leon:001

CREATE TABLE PUBLIC.USUARIOS (
    ID NUMERIC(19) NOT NULL,
    NOME VARCHAR(100) NOT NULL,
    EMAIL VARCHAR(80) NOT NULL,
    LOGIN_ACESSO VARCHAR(32) NOT NULL,
    SENHA VARCHAR(64) NOT NULL,
    PERFIL CHAR(1) NOT NULL,
    CONSTRAINT PK_USUARIOS PRIMARY KEY(ID)
);

CREATE SEQUENCE PUBLIC.SEQ_USUARIOS START 1;

CREATE INDEX IDX_USUARIOS_LOGIN_SENHA ON PUBLIC.USUARIOS (LOGIN_ACESSO ASC, SENHA ASC);

--rollback DROP SEQUENCE PUBLIC.SEQ_USUARIOS;
--rollback DROP TABLE PUBLIC.USUARIOS CASCADE;