export class PageInfo {
  count: number;    // Quantidade total de registros
  pageSize: number; // Quantidade de registros na página atual
  limit: number;    // Quantidade de registros por página
  offset: number;   // Número da página
}
